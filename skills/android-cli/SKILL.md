---
name: android-cli
description: ADB emulator & app debugging
---

## Commands
- `adb devices` -> status check.
- `adb logcat -d` -> read log traces.
- `adb shell am start` -> start activity.

## Rules
- Execute emulator command in background mode.
